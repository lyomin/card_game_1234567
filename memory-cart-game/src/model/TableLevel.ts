export class TableLevel {
    width: number;
    height: number;
    name: string;
    constructor(width: number, height: number, name: string) {
        this.width = width;
        this.height = height;
        this.name = name;
    }
}